CLIENT_ID=your_twitch_client_id
# Local / debug only — prefer TOKEN_PROXY_URL for release builds.
SECRET=your_twitch_client_secret
# Optional production proxy that returns Twitch client-credentials JSON.
# TOKEN_PROXY_URL=https://nice-tv-token-proxy.example.workers.dev
